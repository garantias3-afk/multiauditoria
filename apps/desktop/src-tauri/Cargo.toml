[package]
name = "robot-desktop"
version = "0.1.0"
description = "Thin desktop client for the local Robot Core"
authors = ["multiauditoria"]
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "robot_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

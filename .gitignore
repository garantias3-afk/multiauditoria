.DS_Store
.idea/
.vscode/
__pycache__/
*.pyc
.pytest_cache/
node_modules/
dist/
build/
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.log
camino-a/runtime/dist/
camino-a/runtime/outputs/
camino-a/runtime/work/

# Agregado por Claude: este repo es PÚBLICO. Estas carpetas contienen
# detalles internos de tus otros repos privados (camino-z, robot-os-mellizo,
# openclaw) y artefactos empaquetados grandes — no deberían subirse a GitHub.
shared/audits/
deliverables/


# Graphify: grafo de conocimiento local, se regenera solo
graphify-out/

# Backups locales pre-edicion: nunca deben entrar al repo
*.bak
*.bak[0-9]*
*.backup
*.backup-*

# OT-R (2026-08-03, decision de Mariano): la config de asignaciones lleva la
# topologia de proveedores (provider_id x43 modelos, ruteo de 9 slots). El
# archivo REAL vive versionado en el repo PRIVADO camino-n
# (intercambio/camino_n.assignments.json, junto a la tabla que lo genera);
# en este repo publico solo entra el ejemplo con placeholders.
camino-a/runtime/config/camino_n.assignments.json
